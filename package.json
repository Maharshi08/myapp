{
  "name": "myapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'Skipping build step, no frontend assets'",
    "test": "echo 'No tests defined' && exit 0"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
